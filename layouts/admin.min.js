setInterval(serverTime,1E3);var timestamp=$(".server-time").data("timestamp"),datetime;function serverTime(){datetime=moment.unix(timestamp);$(".server-time").html(moment().format("LLLL"));timestamp+=1}serverTime();
